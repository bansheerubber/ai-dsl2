function main(): int {
	// let frog: int = (1 * 5 + 1 && 20 && 20 - 20) || (1 + 1 - 2);

	let test: int = 5;
	if (test < 5) {
		let value: int = 1;
	} else if (test < 10) {
		let value: int = 2;
	} else if (test < 15) {
		let value: int = 3;
	} else {
		let value: int = 4;
	}

	return 0;
}
